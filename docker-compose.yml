version: "3"

services:
  imgpush:
    container_name: imgpush   
    image: hauxir/imgpush:latest
    restart: unless-stopped
    ports:
      - "5000:5000/tcp"
    volumes:
      - './config:/images'
